<%= render :show, local: {address: @address} %>

<%= link_to t('shopping.edit'), edit_shopping_cart_address_path(@address.cart, @address) %> |
<%= link_to t('shopping.back'), shopping_cart_addresses_path(@address.cart) %>
