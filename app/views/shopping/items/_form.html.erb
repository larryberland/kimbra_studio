<%= form_for(@shopping_item) do |f| %>
  <% if @shopping_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shopping_item.errors.count, "error") %> prohibited this shopping_item from being saved:</h2>

      <ul>
      <% @shopping_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cart %><br />
    <%= f.text_field :cart %>
  </div>
  <div class="field">
    <%= f.label :offer %><br />
    <%= f.text_field :offer %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
