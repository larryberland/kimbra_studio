<%= button_to 'Reorder offers', admin_customer_email_offers_path(@admin_customer_email.tracking), method: :get, title: 'View and reorder all offers for this email' %>
<% btn_message = @admin_customer_email.sent_at? ? "This email already sent on #{time_short_index @admin_customer_email.sent_at}. Click to send again." : 'If all offers are fixed and in the right order, click me to send the offer email!' %>
<%= button_to btn_message,
              send_offers_admin_customer_email_url(@admin_customer_email),
              method: :post %>