<h1> Addresses </h1>

<ul>
  <% for address in @addresses %>
    <li><br/>
      <%= render :partial => '/shared/compact_address', :locals => { :shopping_address => address } %>
      <%= link_to "Edit", edit_studio_address_path(address) %> |
      <%= link_to "Remove", studio_address_path(address), :confirm => 'Are you sure?', :method => :delete %>
    </li>
  <% end %>
</ul>

<p><%= link_to "New Address", new_studio_address_path, :class => 'b-link' %></p>
<p><%= link_to 'Back', studio_overview_path %></p>
