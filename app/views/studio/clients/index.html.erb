<h1>Listing studio_clients</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Phone number</th>
    <th>Active</th>
    <th>Address</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @studio_clients.each do |studio_client| %>
  <tr>
    <td><%= studio_client.name %></td>
    <td><%= studio_client.email %></td>
    <td><%= studio_client.phone_number %></td>
    <td><%= studio_client.active %></td>
    <td><%= studio_client.address %></td>
    <td><%= link_to 'Show', studio_client %></td>
    <td><%= link_to 'Edit', edit_studio_client_path(studio_client) %></td>
    <td><%= link_to 'Destroy', studio_client, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Client', new_studio_client_path %>
