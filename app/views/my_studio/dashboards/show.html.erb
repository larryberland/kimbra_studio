<div class='container'>
  <div class='motivation'>
    <%= portrait_uploaded_days_ago_text %>
  </div>
  <%= button_to 'Upload some now!', new_my_studio_session_path, method: :get %>
  <div class='motivation'>
      Total revenue so far: <%= number_to_currency @sum_purchases %> Your commmission (at <%= @commission_rate %>%): <%= number_to_currency @total_commissions %>
    </div>
</div>

<div class='container'>
  <%= render 'my_studio/dashboards/sessions', :sessions => @last_seven_days %>
</div>

<div class='container'>
  <%= render 'my_studio/dashboards/emails', :sessions => @last_seven_days %>
</div>

<%= link_to t(:back), my_studio_overview_path %>