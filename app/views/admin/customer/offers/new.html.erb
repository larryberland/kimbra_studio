<h1><%= t(:admin_customer_offers_new_title) %></h1>

<div class="platypus-form-container">
  <%= render 'form' %>

  <%= link_to t(:back), admin_customer_email_offers_path(@email), class: "btn btn-primary" %>
</div>