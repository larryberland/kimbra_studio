<div class='side-menu'>
  <table>
    <thead>
    <th><%= @item.try(:to_image_span) %></th>
    </thead>
    <tbody>
    <tr><td>Custom Item:</td></tr>
    <tr><td><%= cw_div_image_only(@item.front, :image => :image_custom, :version => :original) %></td></tr>
    <tr><td>Resized Portrait:</td></tr>
    <tr><td><%= cw_div_image_only(@item.front, :image => :image_stock, :version => :original) %></td></tr>
    <tr><td>Kimbra Part:</td></tr>
    <tr><td><%= cw_div_image_only(@item.part, :image => :image_part) %></td></tr>
    <tr><td>Kimbra Piece:</td></tr>
    <tr><td><%= cw_div_image_only(@item.offer.piece) %></td></tr>
    </tbody>
  </table>
</div>
