<div class='side-menu'>
  <table>
    <tr><td>Custom Item:</td></tr>
    <tr><td><%= cw_div_image(@item.front, :image => :image_custom, :version => :original) %></td></tr>
    <tr><td>Resized Portrait:</td></tr>
    <tr><td><%= cw_div_image(@item.front, :image => :image_stock, :version => :original) %></td></tr>
    <tr><td>Kimbra Part:</td></tr>
    <tr><td><%= cw_div_image(@item.part, :image => :image_part) %></td></tr>
    <tr><td>Kimbra Piece:</td></tr>
    <tr><td><%= cw_div_image(@item.offer.piece) %></td></tr>
  </table>
</div>
