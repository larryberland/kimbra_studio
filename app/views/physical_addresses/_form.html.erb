<%= form_for(@physical_address) do |f| %>
  <% if @physical_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@physical_address.errors.count, "error") %> prohibited this physical_address from being saved:</h2>

      <ul>
      <% @physical_address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
