<%= form_for(@phone_type) do |f| %>
  <% if @phone_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_type.errors.count, "error") %> prohibited this phone_type from being saved:</h2>

      <ul>
      <% @phone_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
